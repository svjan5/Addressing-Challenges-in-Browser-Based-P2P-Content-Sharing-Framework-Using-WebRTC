peer.nodeDetails.makeQuery(Math.pow(2,1))  	 ----------------------------------------------------------------------->>>>>608
peer.nodeDetails.makeQuery(Math.pow(2,1))  	 ----------------------------------------------------------------------->>>>>642
peer.nodeDetails.makeQuery(Math.pow(2,2))  	 ----------------------------------------------------------------------->>>>>605
peer.nodeDetails.makeQuery(Math.pow(2,2))  	 ----------------------------------------------------------------------->>>>>608
peer.nodeDetails.makeQuery(Math.pow(2,3))  	 ----------------------------------------------------------------------->>>>>604
peer.nodeDetails.makeQuery(Math.pow(2,3))  	 ----------------------------------------------------------------------->>>>>616
peer.nodeDetails.makeQuery(Math.pow(2,4))  	 ----------------------------------------------------------------------->>>>>604
peer.nodeDetails.makeQuery(Math.pow(2,4))  	 ----------------------------------------------------------------------->>>>>613
peer.nodeDetails.makeQuery(Math.pow(2,5))  	 ----------------------------------------------------------------------->>>>>595
peer.nodeDetails.makeQuery(Math.pow(2,5))  	 ----------------------------------------------------------------------->>>>>615
peer.nodeDetails.makeQuery(Math.pow(2,6))  	 ----------------------------------------------------------------------->>>>>605
peer.nodeDetails.makeQuery(Math.pow(2,6))  	 ----------------------------------------------------------------------->>>>>670
peer.nodeDetails.makeQuery(Math.pow(2,7))  	 ----------------------------------------------------------------------->>>>>589
peer.nodeDetails.makeQuery(Math.pow(2,7))  	 ----------------------------------------------------------------------->>>>>593
peer.nodeDetails.makeQuery(Math.pow(2,8))  	 ----------------------------------------------------------------------->>>>>599
peer.nodeDetails.makeQuery(Math.pow(2,8))  	 ----------------------------------------------------------------------->>>>>603
peer.nodeDetails.makeQuery(Math.pow(2,9))  	 ----------------------------------------------------------------------->>>>>600
peer.nodeDetails.makeQuery(Math.pow(2,9))  	 ----------------------------------------------------------------------->>>>>612
peer.nodeDetails.makeQuery(Math.pow(2,10))  	 ----------------------------------------------------------------------->>>>>593
peer.nodeDetails.makeQuery(Math.pow(2,10))  	 ----------------------------------------------------------------------->>>>>606
peer.nodeDetails.makeQuery(Math.pow(2,11))  	 ----------------------------------------------------------------------->>>>>593
peer.nodeDetails.makeQuery(Math.pow(2,11))  	 ----------------------------------------------------------------------->>>>>715
peer.nodeDetails.makeQuery(Math.pow(2,12))  	 ----------------------------------------------------------------------->>>>>589
peer.nodeDetails.makeQuery(Math.pow(2,12))  	 ----------------------------------------------------------------------->>>>>617
peer.nodeDetails.makeQuery(Math.pow(2,13))  	 ----------------------------------------------------------------------->>>>>619
peer.nodeDetails.makeQuery(Math.pow(2,13))  	 ----------------------------------------------------------------------->>>>>642
peer.nodeDetails.makeQuery(Math.pow(2,14))  	 ----------------------------------------------------------------------->>>>>620
peer.nodeDetails.makeQuery(Math.pow(2,14))  	 ----------------------------------------------------------------------->>>>>640
peer.nodeDetails.makeQuery(Math.pow(2,15))  	 ----------------------------------------------------------------------->>>>>627
peer.nodeDetails.makeQuery(Math.pow(2,15))  	 ----------------------------------------------------------------------->>>>>644
peer.nodeDetails.makeQuery(Math.pow(2,16))  	 ----------------------------------------------------------------------->>>>>614
peer.nodeDetails.makeQuery(Math.pow(2,16))  	 ----------------------------------------------------------------------->>>>>696